# LEVEL 3《静止之城》· 完整规格

> **文档用途**：LEVEL 3 的实现规格、文案、数值与验算的**合并文档**。
> **配套**：`05-游戏原型设计文档.md`（总设计）、`06`–`08`（LEVEL 1）、`09`–`11`（LEVEL 2）
> **状态**：本文件完成后，游戏线 LEVEL 1–3 全部完稿。

---

## 0. 设计目标

### 0.1 LEVEL 3 要验证什么

LEVEL 1 教"时间会用完"，LEVEL 2 教"你救错地方会死得更快"。

**LEVEL 3 要教一件前两关都教不了的事：**

> **不是所有故障都一直在。**
> **聪明的工程师会等。**

### 0.2 相对前两关的关键差异

| 项 | LEVEL 1 | LEVEL 2 | LEVEL 3 |
|---|---|---|---|
| 故障形态 | 持续 | 持续且恶化 | **间歇性（有节奏）** |
| 失败线 | 时限 | 时限 + 数据库 | 时限 + **堆内存** |
| 核心新机制 | 重启陷阱 | 扩容反向 | **平静期 / 卡顿期** |
| 时限 | 15 分钟 | 40 分钟 | **60 分钟** |
| 视界带宽 | 3 | 3 | 3 |
| 主修手段 | 改代码发版 | 预热 + 发版 | **删除大对象 + 发版** |

---

## 1. 数据结构与常量

### 1.1 扩展字段

```typescript
interface IncidentStateL3 extends IncidentState {
  // ── 时间节奏 ──
  cycleIndex: number;        // 当前是第几个周期（从 0 开始）
  isStallPhase: boolean;     // 当前是否处于卡顿期

  // ── 内存 ──
  heapUsage: number;         // 0–100，初值 60。>= 100 触发 OOM
  heapGrowthRate: number;    // 每个卡顿期的增长量，初值 8

  // ── 重启 ──
  restartCount: number;      // 每次重启 heapUsage 回落到 60，但 heapGrowthRate × 1.5

  // ── 定位 ──
  bigObjectFound: boolean;   // 是否已定位到那个永不过期的大对象
}
```

### 1.2 常量

| 常量 | 值 | 说明 |
|---|---|---|
| `IMPACT_THRESHOLD` | **150** | 达到即失败 |
| `INITIAL_TIME_LIMIT` | **60** 分钟 | — |
| `CYCLE_LENGTH` | **10** 分钟 | 一个完整周期 |
| `STALL_DURATION` | **3** 分钟 | 每周期内的卡顿期 |
| `STALL_SEVERITY` | **4.0** /分 | 卡顿期的 Impact 增速 |
| `CALM_SEVERITY` | **0.3** /分 | 平静期的 Impact 增速 |
| `HEAP_INITIAL` | **60** | 初始堆占用 |
| `HEAP_GROWTH` | **8** /卡顿期 | 每个卡顿期增长 |
| `HEAP_AFTER_RESTART` | **60** | 重启后回落值 |
| `RESTART_GROWTH_MULT` | **1.5** | 重启后增长倍率（累乘） |
| `STALL_TIME_MULT` | **2.0** | 卡顿期诊断卡时间成本倍率 |
| `STALL_IMPACT_BONUS` | **1.0** | 卡顿期 Impact 额外倍率（见 §3.2） |

---

## 2. 核心机制：平静期与卡顿期

### 2.1 时间结构

```
周期 0:  [0, 3)  卡顿    [3, 10)  平静
周期 1:  [10, 13) 卡顿   [13, 20) 平静
周期 2:  [20, 23) 卡顿   [23, 30) 平静
周期 3:  [30, 33) 卡顿   [33, 40) 平静
周期 4:  [40, 43) 卡顿   [43, 50) 平静
周期 5:  [50, 53) 卡顿   [53, 60) 平静
```

```javascript
function isStall(t) {
  return (t % CYCLE_LENGTH) < STALL_DURATION;
}
function severityAt(t) {
  return isStall(t) ? STALL_SEVERITY : CALM_SEVERITY;
}
```

### 2.2 卡顿期的惩罚

| 行为 | 平静期 | 卡顿期 |
|---|---|---|
| **诊断卡** | 正常时间成本 | **时间成本 × 2** |
| **修复卡** | 可用 | **不可用（按钮置灰）** |
| **看 GC 日志** | 可用 | ✅ **唯一不受惩罚的诊断卡** |
| **重启服务** | 可用 | 可用 |
| **同步进展 / 向上汇报** | 可用 | 可用 |

> **「看 GC 日志」在卡顿期不受惩罚**，因为 GC 日志是异步落盘的，不依赖系统响应。

### 2.3 玩家的时间账

一局 60 分钟 = 6 个周期 = **18 分钟卡顿 + 42 分钟平静**。

**修复卡（改代码发版，8 分钟）只能在平静期执行。** 而平静期最长只有 7 分钟——**跨周期的发版是允许的，但会在中途撞上卡顿期**。

```javascript
// 发版跨越卡顿期时：时间正常推进，但玩家会看到 Impact 在那 3 分钟里加速
```

**这是本关最核心的张力**：你必须在平静期开始发版，然后**熬过中间那 3 分钟**。

---

## 3. 双计量模型

### 3.1 Impact

```
impact += severityAt(t) × mitigationFactor × Δt
```

| 阶段 | 强度 | 说明 |
|---|---|---|
| 卡顿期 | **4.0** /分 | 全站响应超时 |
| 平静期 | **0.3** /分 | 少量残留影响 |

### 3.2 堆内存

```
每个卡顿期结束时：heapUsage += heapGrowthRate × (1.5 ^ restartCount)
```

**注意**：堆内存**只在卡顿期增长**——因为卡顿就是 Full GC 的表现，而 Full GC 说明堆快满了。

| 事件 | 效果 |
|---|---|
| 卡顿期结束 | `heapUsage += heapGrowthRate` |
| 使用重启 | `heapUsage = 60`，`heapGrowthRate ×= 1.5` |
| 删除大对象（修复） | `heapGrowthRate = 0`，停止增长 |
| `heapUsage >= 100` | **OOM → 立即失败** |

### 3.3 被动失败曲线

不操作的情况下（**按卡顿期结束时刻标注**）：

| 时刻 | 卡顿期数 | 累计 Impact | 堆内存 |
|---|---|---|---|
| 0:03 | 1 | 12.0 | 68 |
| 0:13 | 2 | 26.1 | 76 |
| 0:23 | 3 | 40.2 | 84 |
| 0:33 | 4 | 54.3 | 92 |
| 0:43 | 5 | 68.4 | **100 → OOM** |

**被动玩家在 t = 43 分钟触发 OOM，此时 Impact 只累积到 68.4（阈值 150）。**

> **结论**：LEVEL 3 的被动失败是 **OOM**，不是超时，也不是 Impact 破线。
> 这与 LEVEL 1（死于时间）、LEVEL 2（死于数据库）形成三级递进。
>
> **Impact 条在这一关同样不会成为失败原因**——它是"你影响了多少请求"的账，不是倒计时。

---

## 4. 卡牌规格（差异与新增）

### 4.1 卡牌总表（19 张）

| ID | 名称 | 类别 | 时间 | 精力 | 上限 | 相对前作 |
|---|---|---|---|---|---|---|
| `C_MONITOR` | 看监控大盘 | observe | 0 | 0 | ∞ | 扩展显示堆内存 |
| `C_LOG` | 看日志 | observe | 1 | 0 | 2 | 保留 |
| `C_GC_LOG` | **看 GC 日志** | observe | 2 | 1 | 2 | **新 · 卡顿期唯一不受罚** |
| `C_HEAP_DUMP` | **导出堆快照** | diagnose | 4 | 2 | 1 | **新 · 定位大对象** |
| `C_TRACE` | 链路追踪 | diagnose | 2 | 1 | ∞ | 保留 |
| `C_BISECT` | 二分定位 | diagnose | 3 | 1 | 2 | 保留 |
| `C_DIFF` | 比对变更 | diagnose | 2 | 1 | 1 | **假线索（本周有发布）** |
| `C_CACHE_SCAN` | **扫描缓存对象** | diagnose | 3 | 1 | 1 | **新 · 正解前置** |
| `C_RATELIMIT` | 限流 | mitigate | 1 | 1 | 1 | 保留 |
| `C_DEGRADE` | 降级 | mitigate | 2 | 1 | 1 | 保留 |
| `C_SCALE` | 扩容 | mitigate | 3 | 2 | 2 | **本关有效！** |
| `C_RESTART` | 重启服务 | mitigate | 1 | 1 | ∞ | **暂时有效但加速泄漏** |
| `C_GC_TUNE` | **调大堆内存** | mitigate | 3 | 2 | 1 | **新 · 饮鸩止渴** |
| `C_DEPLOY` | 改代码 + 发版 | fix | 8 | 3 | 1 | **正解（需已定位）** |
| `C_SYNC` | 同步进展 | communicate | 1 | 1 | 3 次 | 保留 |
| `C_ESCALATE` | 向上汇报 | communicate | 2 | 1 | 1 次 | 保留 |
| `C_VISION` | 开启视界 | — | 0 | 0 | 带宽 3 | 保留 |

### 4.2 新增卡牌详解

#### `C_GC_LOG` · 看 GC 日志 ★

```javascript
effect: (s) => ({
  logKey: s.usedCards.get('C_GC_LOG') ? 'gc.second' : 'gc.first',
  effective: true
})
```

第一次使用返回：

```
Full GC 统计（最近一小时）：

  次数        47
  平均耗时     2.8 秒
  最长耗时     3.4 秒

  老年代占用：  92% → 96% → 94% → 97% …

Full GC 的间隔是规律的：**每 10 分钟一次**。
```

**这张卡是定位问题的钥匙，而且是卡顿期唯一能用的诊断卡。**

#### `C_HEAP_DUMP` · 导出堆快照 ★

```javascript
requires: (s) => s.evidence.has('E_GC'),   // 需先看 GC 日志
effect: () => ({ logKey: 'heap.dump', effective: true })
```

返回：

```
堆快照（占用最大的前 5 个对象）：

  1. java.util.HashMap         1.8 GB   ←
  2. byte[]                    120 MB
  3. com.xxx.OrderCache        86 MB
  ...

第 1 个对象的引用链：

  OrderCacheService.staticCache
    └─ HashMap
         └─ 2,847,392 个 Entry
```

**这一条线索指向根因。**

#### `C_CACHE_SCAN` · 扫描缓存对象 ★★ 正解前置

```javascript
requires: (s) => s.evidence.has('E_HEAP'),
effect: (s) => {
  s.bigObjectFound = true;
  s.rootCauseConfirmed = true;
  return { logKey: 'cache.found', effective: true };
}
```

返回：

```
静态缓存 OrderCacheService.staticCache

  条目数：      2,847,392
  最早写入：    2021-06-14
  过期策略：    **无**
  清理逻辑：    **无**

这个 Map 从服务启动那天起就在。
三年了。

它里面的东西，一条都没有被清理过。
```

#### `C_GC_TUNE` · 调大堆内存 ⚠️ 饮鸩止渴

```javascript
effect: (s) => {
  s.heapGrowthRate *= 0.7;      // 增长变慢
  s.heapUsage = Math.max(0, s.heapUsage - 15);  // 暂时回落
  return { logKey: 'gc_tune.on', effective: false };  // 永远不计有效
}
```

**看起来是正解**——调大堆内存，Full GC 频率下降，卡顿减少。

**但它是治标**：泄漏还在，只是被推迟了。而且堆越大，一次 Full GC 的 STW 时间越长——**卡顿会变少，但每次更久**。

> **设计意图**：这是本关的"扩容陷阱"对应物。LEVEL 2 用扩容教"救错地方会更快死"，LEVEL 3 用调大堆教"**缓解不等于解决，而且缓解本身有代价**"。

#### `C_SCALE` · 扩容 —— **本关首次有效**

```javascript
effect: (s) => {
  s.scaleCount += 1;
  s.mitigationFactor *= 0.7;
  return { logKey: 'scale.effective', effective: true };
}
```

**为什么本关有效**：瓶颈在应用层（GC 停顿），不在下游。**更多实例 = 每个实例的堆压力更小 = Full GC 频率下降**。

> **这是三关的对照设计**：
> - LEVEL 1：扩容无效（浪费时间）
> - LEVEL 2：扩容**反向**（加速崩溃）
> - LEVEL 3：扩容**有效**（瓶颈真的在应用层）
>
> **同一张卡，三种结果。教的是同一件事：扩容只解决"自己不够用"的问题。**

#### `C_RESTART` · 重启服务 ⚠️ 暂时有效

```javascript
effect: (s) => {
  s.restartCount += 1;
  s.heapUsage = HEAP_AFTER_RESTART;         // 回落到 60
  s.heapGrowthRate *= RESTART_GROWTH_MULT;  // 但增长 ×1.5
  s.evidence.clear();
  s.rootCauseConfirmed = false;
  s.evidenceLost = true;
  return { logKey: 'restart.temporary', effective: false };
}
```

**本关的重启是"看起来真的有用"的**：堆内存从 92% 回落到 60%，卡顿立刻消失。

**但**：
1. 泄漏速度 ×1.5
2. 证据清空（与 LEVEL 1、2 一致）
3. **最狠的一点**：重启后大对象会被**重新加载**，而且加载过程本身会让堆增长更快

---

## 5. 参考通关路径

> **⚠️ 本节的数值已由 `tools/verify-balance-l3.ps1` 验算，15 项断言全部 PASS。**
> **此前版本中的 62.6 / 55.8 等数字为手工粗算，漏算了限流的缓解系数，已全部废弃。**

### 路径 A · 标准解（预期 A）

| 回合 | 行动 | 时刻 | 阶段 | Impact | 堆内存 |
|---|---|---|---|---|---|
| 1 | 看监控大盘 | 0:00 | 卡顿 | 0 | 60 |
| 2 | **看 GC 日志** | 0:02 | 卡顿 | 8.0 | 60 |
| 3 | 等（跳过） | 0:03 | 平静 | 12.0 | **68** |
| 4 | 导出堆快照 | 0:07 | 平静 | 13.2 | 68 |
| 5 | 扫描缓存对象 | 0:10 | 卡顿 | 14.1 | 68 |
| 6 | 同步进展 | 0:11 | 卡顿 | 18.1 | 68 |
| 7 | 限流 | 0:12 | 卡顿 | 22.1 | 68 |
| 8 | 等（跳过） | 0:20 | 卡顿 | 25.15 | **76** |
| 9 | **改代码 + 发版** | 0:28 | 平静 | **31.9** | **84** |
| — | **通关** | 0:28 | — | **31.9** | **84** |

**评级 A**（根治 + Impact < 75 + 未重启 + 未超频）

> **注意第 9 回合**：发版从 0:20 开始，**正好撞上卡顿期的开头**。那 3 分钟里 Impact 以 2.0/分推进（限流后），贡献了 6 点。
>
> **这就是本关要教的：发版要 8 分钟，而平静期只有 7 分钟——你永远躲不开卡顿期。但你可以在"什么时候开始"上做选择。**

### 路径 A+ · 算准发版时机（预期 S）

**与路径 A 唯一的区别：第 8 回合只等 1 分钟，等卡顿期结束后立刻发版。**

| 回合 | 行动 | 时刻 | 阶段 | Impact | 堆内存 |
|---|---|---|---|---|---|
| 1–7 | 同路径 A | 0:12 | 卡顿 | 22.1 | 68 |
| 8 | 等（**等卡顿期结束**） | 0:13 | 平静 | 24.1 | **76** |
| 9 | **改代码 + 发版** | 0:21 | 卡顿 | **27.15** | **76** |
| — | **通关** | 0:21 | — | **27.15** | **76** |

**评级 S**（Impact < 30）

> **两条路径的差别只有一次「等 1 分钟 vs 等 8 分钟」，但结果差 4.75 点 Impact（约 114 个用户请求）。**
>
> 原因：
> - 路径 A 的发版窗口 = **3 分钟卡顿 + 5 分钟平静**
> - 路径 A+ 的发版窗口 = **7 分钟平静 + 1 分钟卡顿**
>
> **这就是本关唯一的核心技巧：把发版安排成"尽量多的平静 + 尽量少的卡顿"。**
>
> **「等」是一个决策，不是一个放弃。**

### 路径 B · 视界解（预期 S）

| 回合 | 行动 | 时刻 | 阶段 | Impact | 堆内存 |
|---|---|---|---|---|---|
| 1 | 看监控大盘 | 0:00 | 卡顿 | 0 | 60 |
| 2 | 看 GC 日志 | 0:02 | 卡顿 | 8.0 | 60 |
| 3 | **开启视界** | 0:02 | 卡顿 | 8.0 | 60 |
| 4 | 等（跳过） | 0:03 | 平静 | 12.0 | **68** |
| 5 | 限流 | 0:04 | 平静 | 12.3 | 68 |
| 6 | **改代码 + 发版** | 0:12 | 卡顿 | **17.2** | **68** |
| — | **通关** | 0:12 | — | **17.2** | **68** |

**评级 S**（Impact < 30）· 带宽剩余 2

> **注意**：路径 B 比路径 A+ **少用 9 分钟**（12 vs 21），但**评级相同（都是 S）**。
>
> **视界买到的是时间，不是分数。** 这和三关的一贯设计一致。

### 路径 C · 重启后翻盘（预期 B）

| 回合 | 行动 | 时刻 | 阶段 | Impact | 堆内存 |
|---|---|---|---|---|---|
| 1 | 看监控大盘 | 0:00 | 卡顿 | 0 | 60 |
| 2 | **重启服务** | 0:01 | 卡顿 | 4.0 | 60（增长 ×1.5 → 12） |
| 3 | 看 GC 日志 | 0:03 | 平静 | 12.0 | **72** |
| 4 | 开启视界 | 0:03 | 平静 | 12.0 | 72 |
| 5 | 限流 | 0:04 | 平静 | 12.3 | 72 |
| 6 | **改代码 + 发版** | 0:12 | 卡顿 | **17.2** | **72** |
| — | **通关** | 0:12 | — | **17.2** | **72** |

**评级 B**（Impact 本可评 S，**因重启封顶为 B**）

> **重启在本关是"真的有用"的**：堆内存回落到 60，卡顿立刻消失。
>
> **但**：
> 1. 泄漏速度 ×1.5（本局堆峰值从 68 升到 72）
> 2. 证据清空——**如果没开视界，重启后就得从头查**
> 3. **最狠的一点**：重启后大对象会被重新加载，加载过程本身让堆增长更快

### 路径 D · 饮鸩止渴（预期 F）

| 回合 | 行动 | 时刻 | Impact | 堆内存 |
|---|---|---|---|---|
| 1 | 调大堆内存 | 0:03 | 12.0 | 53（增长 ×0.7 → 5.6） |
| 2 | 一直等 | 1:00:00 | **84.6** | **81** |
| — | **超时** | 60:00 | **84.6** | **81** |

**评级 F**（`lose_timeout`）

> 调大堆内存的玩家**活得比被动玩家久**（不会 OOM：被动在 0:43 就死了），**但永远修不好**，最后死于超时。
>
> **这是本关最阴险的一条路：它看起来一直在起作用。**
>
> **而且它有一个隐藏代价**——堆越大，一次 Full GC 的 STW 时间越长。**卡顿会变少，但每次更久。**

---

## 6. 评级与结算

### 6.1 评级阈值

| 评级 | 条件 |
|---|---|
| **S** | 根治 + Impact < 30 + 未重启 + 未超频 |
| **A** | 根治 + Impact < 75 + 未重启 + 未超频 |
| **B** | 根治 + Impact ≥ 75；或 根治但重启过 |
| **C** | 止血通关（本关不可达——无法把强度降到 0） |
| **F** | 失败（OOM / Impact 破线 / 超时） |

### 6.2 结算数据项（新增）

| 项 | 来源 |
|---|---|
| **Full GC 次数** | 统计整局触发的卡顿期数 |
| **最长 STW** | 固定值 3.4 秒（叙事用） |
| **堆内存峰值** | 记录 `maxHeapUsage` |
| **你等待的时间** | 统计"跳过"回合的总时长 |
| **因卡顿损失的响应** | `round(impact × 24)` |

### 6.3 F 级结算文案 · OOM

```
【 F 】

内存溢出

事故时间      {time} / 60 分钟
最终影响值    {impact} / 150
堆内存        100 / 100

Full GC 次数  5
最长 STW      3.4 秒

────────────────────────────

老赵：
「……」
（他在看监控大屏。屏幕上是黑的。）

────────────────────────────

复盘 · 你的时间花在哪了

  看大盘        0 分钟
  看 GC 日志     2 分钟
  等            41 分钟
  改代码发版     0 分钟

  **你花了 41 分钟在等。**

────────────────────────────

复盘 · 等是对的，但你没有等对

这一关的设计是：**故障是间歇性的**。
每 10 分钟里，只有 3 分钟是卡顿的。

所以「等」在这个场景里，是一个正确的决策。
**平静期才是你干活的时间。**

但你的问题不是「等」。
**是你等的时候，什么都没做。**

平静期有 7 分钟。
7 分钟够看一次 GC 日志，够导一次堆快照。

**你没有等。你只是在熬。**

────────────────────────────

复盘 · 关于那个 Map

  OrderCacheService.staticCache
  条目数：2,847,392
  最早写入：2021-06-14
  过期策略：**无**

这个 Map 从服务启动那天起就在。三年了。

它里面的东西，一条都没有被清理过。

**它不是 Bug。**
**它是一个人当年为了"快一点"，写下的一个 static 字段。**

写那行代码的人，大概只花了十秒钟。
```

### 6.4 A 级结算文案 · 复盘

```
【 A 】

事故时间      28 / 60 分钟
最终影响值    31.9 / 150

堆内存峰值    84 / 100
Full GC 次数  2

重启次数      0
视界使用      0 次
有效操作      7 / 9
等待时间      9 分钟

────────────────────────────

老赵：
「……你等了九分钟。」
「那九分钟里，你干了四件事。」
「**但你有一件事没算。**」

────────────────────────────

复盘 · 你少算的那一步

你的发版是从 0:20 开始的。
那一刻，**卡顿期刚刚开始**。

  发版需要 8 分钟。
  你的窗口 = 3 分钟卡顿 + 5 分钟平静。

如果你再等 1 分钟——等卡顿期结束、从 0:13 开始发版——

  你的窗口 = 7 分钟平静 + 1 分钟卡顿。

结果：

  你打出的 Impact    31.9
  你可以打出的 Impact 27.15

**差 4.75 点。大约 114 个用户请求。**

────────────────────────────

复盘 · 一个你没法绕开的算术题

  平静期长度：7 分钟
  发版耗时：  8 分钟

**你永远躲不开卡顿期。**

这不是设计缺陷。这是这道题的条件。

你能选的，只有「让那 8 分钟里包含多少卡顿」：

  最差：3 分钟卡顿 + 5 分钟平静
  最好：1 分钟卡顿 + 7 分钟平静

差的这 2 分钟，就是这一关的全部技术含量。

────────────────────────────

复盘 · 间歇性故障的处理原则

不是所有故障都一直在。

有的故障有节奏：来一阵，走一阵，再来一阵。

处理这类故障，有三条原则：

  1. **不要在故障发作时抢救**
     系统响应慢，你的命令发不出去，诊断卡时间翻倍

  2. **在平静期做诊断**
     那是系统唯一"听得见你说话"的时候

  3. **算清楚下一次发作的时间，再决定什么时候动手**
     发版要 8 分钟。下一个卡顿期什么时候来？

**「等」是一个决策，不是一个放弃。**
```

---

## 7. 数值验算

> **本节全部数值由 `tools/verify-balance-l3.ps1` 机器验算，15 项断言全部 PASS。**
> **调参后必须重跑该脚本。**

### 7.1 被动失败

| 时刻 | 事件 | Impact | 堆内存 |
|---|---|---|---|
| 0:03 | 第 1 个卡顿期结束 | 12.0 | **68** |
| 0:13 | 第 2 个卡顿期结束 | 26.1 | **76** |
| 0:23 | 第 3 个卡顿期结束 | 40.2 | **84** |
| 0:33 | 第 4 个卡顿期结束 | 54.3 | **92** |
| 0:43 | 第 5 个卡顿期结束 | 68.4 | **100 → OOM** |

**被动玩家在 t = 43 分钟 OOM，此时 Impact 只累积到 68.4（阈值 150）。**

> **结论**：LEVEL 3 的被动失败是 **OOM**，不是超时，也不是 Impact 破线。
> **Impact 条在这一关同样不会成为失败原因**——它是"你影响了多少请求"的账。

### 7.2 路径验算

**路径 A（标准解）**——注意限流在 t=0:12 生效，之后所有强度 ×0.5：

```
t=0:00–0:02  卡顿  4.0 × 2         = 8.00    → 8.00
t=0:02–0:03  卡顿  4.0 × 1         = 4.00    → 12.00   （堆 +8 → 68）
t=0:03–0:07  平静  0.3 × 4         = 1.20    → 13.20
t=0:07–0:10  平静  0.3 × 3         = 0.90    → 14.10
t=0:10–0:11  卡顿  4.0 × 1         = 4.00    → 18.10
t=0:11–0:12  卡顿  4.0 × 1         = 4.00    → 22.10
t=0:12–0:13  卡顿  2.0 × 1         = 2.00    → 24.10   （堆 +8 → 76）
t=0:13–0:20  平静  0.15 × 7        = 1.05    → 25.15
t=0:20–0:23  卡顿  2.0 × 3         = 6.00    → 31.15   （堆 +8 → 84）
t=0:23–0:28  平静  0.15 × 5        = 0.75    → 31.90
```

**最终 Impact = 31.90，堆内存 84，t = 28 分钟 → 评级 A。**

**路径 A+（算准时机）**——与 A 的唯一区别是第 8 回合只等 1 分钟：

```
… 至 t=0:12 同路径 A                        → 22.10
t=0:12–0:13  卡顿  2.0 × 1         = 2.00    → 24.10   （堆 +8 → 76）
t=0:13–0:20  平静  0.15 × 7        = 1.05    → 25.15
t=0:20–0:21  卡顿  2.0 × 1         = 2.00    → 27.15
```

**最终 Impact = 27.15，堆内存 76，t = 21 分钟 → 评级 S。**

> **A 与 A+ 的差值 4.75，全部来自"发版窗口里包含了几分钟卡顿"。**

### 7.3 一致性检查清单

| # | 检查项 | 期望值 | 状态 |
|---|---|---|---|
| 1 | 被动不操作，t=43 | 堆内存 100 → OOM | ✅ |
| 2 | 被动不操作，t=33 | Impact = 54.3，堆内存 = 92 | ✅ |
| 3 | 路径 A 结束 | Impact = 31.9，堆内存 84，评级 A | ✅ |
| 4 | 路径 A+ 结束 | Impact = 27.15，堆内存 76，评级 S | ✅ |
| 5 | 路径 B 结束 | Impact = 17.2，堆内存 68，评级 S | ✅ |
| 6 | 路径 C 结束 | Impact = 17.2，堆内存 72，评级 B（重启封顶） | ✅ |
| 7 | 路径 D 结束 | Impact = 84.6，未 OOM，评级 F（超时） | ✅ |
| 8 | 卡顿期诊断卡时间成本 | ×2 | — |
| 9 | 卡顿期修复卡 | 不可用（置灰） | — |
| 10 | `C_GC_LOG` 在卡顿期 | 不受惩罚 | — |
| 11 | 重启后堆内存 | 回落到 60 | ✅ |
| 12 | 重启后增长倍率 | ×1.5 | ✅ |
| 13 | 扩容在本关 | **有效**（`mitigationFactor × 0.7`） | — |
| 14 | 调大堆内存 | 不计有效操作 | — |

> **第 8–10、13–14 项为机制约定，无法由数值脚本验证，需在灰盒原型中人工确认。**

---

## 7.4 本次修正记录

**29 号文档 v1.0 的数值存在三处错误，已全部修正：**

| 位置 | v1.0 的值 | 正确值 | 错因 |
|---|---|---|---|
| §5.1 路径 A 表格 | 62.6 | **31.9** | 未应用限流的缓解系数；且时间列不连续 |
| §7.2 逐段验算 | 55.8 | **31.9** | 同样漏算缓解系数 |
| §7.1 被动曲线 | 用"周期"标注，末行 70.5 | **按卡顿期结束时刻标注，68.4** | 混淆了"周期末"与"卡顿期末" |

**教训**：这三处都是手工推导的产物。**LEVEL 1 和 LEVEL 2 从第一天起就有验算脚本，LEVEL 3 没有——结果就是它错得最离谱。**

> **纪律：任何数值改动，先跑脚本，再改文档。**

---

## 8. 三关对照总表

| 维度 | LEVEL 1 | LEVEL 2 | LEVEL 3 |
|---|---|---|---|
| **故障** | 重复下单 | 缓存雪崩 | Full GC 停顿 |
| **主失败因** | 时间 | 数据库 | **堆内存** |
| **核心教学** | 时间会用完 | 救错地方会更快死 | **不是所有故障都一直在** |
| **扩容** | 无效 | **反向** | **有效** |
| **重启** | 清空 Impact，毁证据 | 不清空，制造惊群 | **暂时有效，加速泄漏** |
| **时限** | 15 分 | 40 分 | 60 分 |
| **Impact 阈值** | 100 | 200 | 150 |
| **评级 S** | < 25 | < 40 | < 30 |
| **评级 A** | < 50 | < 90 | < 75 |

**三关合起来，教会玩家一件事：**

> **每一次事故都不一样。**
> **你学过的每一张卡，都可能在这一关失效、反向、或者刚刚好。**
>
> **唯一能带走的，是判断力。**

---

*文档版本：v1.0 · ✅ 本文件完成后，游戏线 LEVEL 1–3 全部完稿*
